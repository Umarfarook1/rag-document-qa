# Dependencies live in pyproject.toml. This file exists for tools that expect a
# requirements.txt and resolves to the same set, so the two cannot drift.
#
# Base install (numpy, pyyaml, anthropic, python-dotenv, httpx). Enough for the
# fake embedder and the in-memory index:
-e .
#
# For BGE embeddings, FAISS, PDF loaders and the test/lint toolchain:
#   pip install -e ".[dev,all]"
